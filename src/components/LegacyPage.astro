---
import type { LegacyHtmlPage } from '../lib/legacy-html';

interface Props {
  page: LegacyHtmlPage;
}

const { page } = Astro.props;
---

{page.inlineStyles.map((css) => <style is:global set:html={css} />)}
<Fragment set:html={page.bodyHtml} />
